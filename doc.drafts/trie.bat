dot -Tsvg -Gdpi=300 -o trie.svg trie.dot
dot -Tpng -Gdpi=300 -o trie.png trie.dot
rem dot -Tsvg -Gdpi=300 -o trie_flat.svg trie_flat.dot
rem dot -Tpng -Gdpi=300 -o trie_flat.png trie_flat.dot

dot -Tsvg -Gdpi=300 -o trie_brackets.svg trie_brackets.dot
dot -Tpng -Gdpi=300 -o trie_brackets.png trie_brackets.dot

dot -Tsvg -Gdpi=300 -o trie_numbers.svg trie_numbers.dot
dot -Tpng -Gdpi=300 -o trie_numbers.png trie_numbers.dot

dot -Tsvg -Gdpi=300 -o trie_operators.svg trie_operators.dot
dot -Tpng -Gdpi=300 -o trie_operators.png trie_operators.dot

